# Local Image Chat — Pearl Glass Design Baseline

2026-09-07、ユーザーはBの配置・Pearl Glass配色を選択し、Production実装を承認した。この文書は採用済みUIを今後の変更判断の基準として固定する。新しい画面やデザインを提案する文書ではありません。実装と異なる理想論を追加せず、既存のVanilla UI、`public/index.html`、`public/style.css`、既存UIテストを正本として扱います。

## 技術前提

- Vanilla HTML / CSS / JavaScriptを使用する。
- Expressを使用する。
- 既存の`ui-kit.js`、native HTML、Option Pickerを優先する。
- React、Vue、Tailwind、その他のUIフレームワークを導入しない。
- 新規依存は最後の手段とし、既存の依存とブラウザ標準機能を優先する。
- UI変更だけを理由にAPI、履歴形式、状態管理を変更しない。

## 配色とトークン

現在の基本トークンは次のとおりです。

| 用途 | 値 |
| --- | --- |
| ページ背景 | `#e5edf5` |
| カード背景 | `#f8fbfe` |
| 上層面 | `#ffffff` |
| 境界線 | `#cbd6e1` |
| 本文 | `#1c2938` |
| 補助文字 | `#526274` |
| アクセント | `#285bc8` |
| エラー | `#b32d43` |

アクセントは次の用途に限定します。

- 現在選択されている項目
- 主要生成ボタン
- 成功・接続OKは独立した落ち着いたグリーンを使う
- 入力済みを示す小さなインジケーター

次の表現は使用しません。

- 紫ネオン
- 常時発光
- 強いグラデーション
- 太いライム枠
- 全面ライム背景の乱用
- カードの中へ別カードを重ねること

## 生成画面の役割分担

| 領域 | 役割 |
| --- | --- |
| 左カラム | プロンプト、生成設定、LoRAなどの編集 |
| 中央 | 生成画像、選択画像、候補の確認 |
| 画像下部 | Recent履歴の横一覧 |
| 開閉パネル | 読み取り専用メタデータ |

維持する条件は次のとおりです。

- デスクトップは左編集＋右画像の2領域。Recentは画像下部に置く。
- 中央画像を最優先する。
- 左カラムはスクロール領域と、通常フローにある生成フッターを分離する。
- Recentの横スクロールと詳細パネルの縦スクロールを分離する。Gallery拡大は画像＋metadataを同時表示する。
- 主要レイアウトをabsolute配置や負のmarginで成立させない。
- `minmax(0, 1fr)`、`min-width: 0`、`min-height: 0`を適切に使う。
- 生成ボタンや詳細パネルを他要素へ重ねない。

## コンポーネント規則

- 閉じたアコーディオンは1行に固定する。
- 長い値はellipsisで省略し、必要なら`title`で全文を確認できるようにする。
- 本文は13〜14px以上を目安にする。
- モバイル入力欄はiOS自動ズームを避けられるサイズにする。
- アイコン操作も十分なクリック・タップ領域を確保する。
- destructive操作は確認を伴う。
- 一覧へ詳細情報を常時詰め込まない。
- ラベル、入力欄、ボタンの高さと角丸を画面内で揃える。

## 画像配信規則

- 一覧はサムネイルを使用する。
- 原寸は明示的な選択、詳細、拡大時だけ取得する。
- 一覧で原寸を先読みしない。
- サムネイル失敗時だけ原寸へ1回フォールバックする。
- `loading="lazy"`と`decoding="async"`を使用する。
- キャッシュ回避用の時刻・乱数クエリを付けない。
- 画像のアスペクト比を壊さない。

## アクセシビリティとモーション

- アイコン操作は原則`button`を使用する。
- 状態に応じた`aria-label`と`title`を付ける。
- キーボード操作に対応する。
- フォーカス表示を維持する。
- モーダルはEscape、閉じる、キャンセルに対応する。
- 星や三点メニューを親カード選択から分離する。
- `prefers-reduced-motion`へ配慮する。
- 動きはtransformとopacityを中心に、150〜200ms程度に抑える。

## レスポンシブ検証基準

次の表示条件で確認する。

- 1366×768 / 100%
- 1366×768 / 125%
- 1920×1080
- 2560×1440
- 390〜430px幅
- iPhone Safari

確認項目は次のとおりです。

- 横スクロールが発生しない。
- fixed/sticky操作が本文を隠さない。
- 長いモデル名、Checkpoint名、Promptが親幅を押し広げない。
- 画像の縦横比が崩れない。

## Glassと主要設定

透過とblurはnavigation・操作バー・Recentの面に限定し、長文入力やmetadataの背景は安定した明度を保つ。reduced transparency/motionでは効果を減らす。主要設定は解像度プリセット、Sampler、Scheduler、Steps、CFG、Seed、枚数。Sampler/Schedulerは既存Runtime候補と検索・Favorite・recentを使う。LoRAは小型thumbnail、folder再開位置、±と数値によるweight調整。
