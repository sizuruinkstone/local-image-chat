# Local Image Chat v3.0 Design Baseline

この文書は、現在承認済みのLocal Image Chat v3.0 UIを今後の変更判断の基準として固定するためのものです。新しい画面やデザインを提案する文書ではありません。実装と異なる理想論を追加せず、既存のVanilla UI、`public/index.html`、`public/style.css`、既存UIテストを正本として扱います。

## 技術前提

- Vanilla HTML / CSS / JavaScriptを使用する。
- Expressを使用する。
- 既存の`ui-kit.js`、native HTML、Option Pickerを優先する。
- React、Vue、Tailwind、その他のUIフレームワークを導入しない。
- 新規依存は最後の手段とし、既存の依存とブラウザ標準機能を優先する。
- UI変更だけを理由にAPI、履歴形式、状態管理を変更しない。

## 配色とトークン

現在の基本トークンは次のとおりです。

| 用途 | 値 |
| --- | --- |
| ページ背景 | `#080808` |
| カード背景 | `#111111` |
| 上層面 | `#181818`前後 |
| 境界線 | `#2A2A2A` |
| 本文 | `#F5F5F5` |
| 補助文字 | `#9A9A9A` |
| アクセント | `#B7E82E` |
| エラー | `#FF6B78` |

アクセントは次の用途に限定します。

- 現在選択されている項目
- 主要生成ボタン
- 成功・接続OKの状態
- 入力済みを示す小さなインジケーター

次の表現は使用しません。

- 紫ネオン
- 常時発光
- 強いグラデーション
- 太いライム枠
- 全面ライム背景の乱用
- カードの中へ別カードを重ねること

## 生成画面の役割分担

| 領域 | 役割 |
| --- | --- |
| 左カラム | プロンプト、生成設定、LoRAなどの編集 |
| 中央 | 生成画像、選択画像、候補の確認 |
| 右カラム | 履歴と読み取り専用メタデータ |

維持する条件は次のとおりです。

- デスクトップは3カラムとする。
- 中央画像を最優先する。
- 左カラムはスクロール領域と、通常フローにある生成フッターを分離する。
- 右カラムは履歴スクロールと詳細パネルを分離する。
- 主要レイアウトをabsolute配置や負のmarginで成立させない。
- `minmax(0, 1fr)`、`min-width: 0`、`min-height: 0`を適切に使う。
- 生成ボタンや詳細パネルを他要素へ重ねない。

## コンポーネント規則

- 閉じたアコーディオンは1行に固定する。
- 長い値はellipsisで省略し、必要なら`title`で全文を確認できるようにする。
- 本文は13〜14px以上を目安にする。
- モバイル入力欄はiOS自動ズームを避けられるサイズにする。
- アイコン操作も十分なクリック・タップ領域を確保する。
- destructive操作は確認を伴う。
- 一覧へ詳細情報を常時詰め込まない。
- ラベル、入力欄、ボタンの高さと角丸を画面内で揃える。

## 画像配信規則

- 一覧はサムネイルを使用する。
- 原寸は明示的な選択、詳細、拡大時だけ取得する。
- 一覧で原寸を先読みしない。
- サムネイル失敗時だけ原寸へ1回フォールバックする。
- `loading="lazy"`と`decoding="async"`を使用する。
- キャッシュ回避用の時刻・乱数クエリを付けない。
- 画像のアスペクト比を壊さない。

## アクセシビリティとモーション

- アイコン操作は原則`button`を使用する。
- 状態に応じた`aria-label`と`title`を付ける。
- キーボード操作に対応する。
- フォーカス表示を維持する。
- モーダルはEscape、閉じる、キャンセルに対応する。
- 星や三点メニューを親カード選択から分離する。
- `prefers-reduced-motion`へ配慮する。
- 動きはtransformとopacityを中心に、150〜200ms程度に抑える。

## レスポンシブ検証基準

次の表示条件で確認する。

- 1366×768 / 100%
- 1366×768 / 125%
- 1920×1080
- 2560×1440
- 390〜430px幅
- iPhone Safari

確認項目は次のとおりです。

- 横スクロールが発生しない。
- fixed/sticky操作が本文を隠さない。
- 長いモデル名、Checkpoint名、Promptが親幅を押し広げない。
- 画像の縦横比が崩れない。
